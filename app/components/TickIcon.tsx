'use client';

import { Tick01Icon } from '@houstonicons/pro';

export function TickIcon() {
  return (
    <Tick01Icon
      type={'rounded'}
      variant={'bulk'}
      size={24}
      color={'#4ADE80'}
      strokeWidth={2}
    />
  );
}
